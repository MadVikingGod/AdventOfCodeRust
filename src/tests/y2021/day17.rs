use crate::y2021::day17::*;

#[test]
fn test_nothing() -> Result<(), String> {
    Ok(())
}

#[test]
fn test_x_steps() -> Result<(), String> {
    let _hm = x_steps(20, 30);

    Ok(())
    // Err(format!("{:?}", hm))
}
